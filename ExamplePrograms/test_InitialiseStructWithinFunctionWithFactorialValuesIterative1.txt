struct example
{
    first : int;
    second : int;
    third : int;
}

function main : int ()
{
    retVal : example;
    retVal = coolFunction();

    result : int;
    result = retVal.first;

    yesOrNo : int;
    if (result == 6)
    {
        yesOrNo = 89;
    }
    else
    {
        yesOrNo = 78;
    }
    return yesOrNo;
}

function coolFunction : example ()
{
    next : example;
    next.first = factorial(3);
    next.second = factorial(4);
    next.third = factorial(5);
    return next;
}

function factorial : int (n : int)
{
    value : int;
    value = 1;

    index : int;
    index = 1;
    
    while (index <= n)
    {
        value = value * index;
        index = index + 1;
    }

    return value;
}